jaffle_shop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      password:
      port: 5432
      dbname: jaffle_shop
      threads: 8
      # optional:
      # extensions: [httpfs, parquet]
